function runSimulation(parameters) {
    // Perform some operations to simulate a process
    let simulation = {
        success: true,
        data: [1, 2, 3, 4]
    };
    
    // Return the simulation result
    return simulation;
}

// Calling the function
let result = runSimulation(someParameters);
console.log(result);  // Output: { success: true, data: [1, 2, 3, 4] }
